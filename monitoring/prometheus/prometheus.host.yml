# Use with docker-compose.observability-full.yml — API runs on the host (not in Compose).
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: providers-app
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:4000"]
