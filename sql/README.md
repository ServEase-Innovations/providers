# Moved

Provider SQL patches are consolidated under:

**[`../../../database/sql/`](../../../database/sql/)** (files prefixed `050_providers_`, `060_`, …)

Add new provider DDL there and run `npm run db:migrate` from the monorepo root.
